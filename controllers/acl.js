module.exports = function (app, db, config, passport) {
  /*
  var prefix = config.prefix || '';

  app.all(prefix + '/*', function (req, res, next) {
    if (!req.user) res.redirect(prefix + '/auth/login');
    else next();
  });
  */
};
